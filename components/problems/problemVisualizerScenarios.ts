export type ScenarioVisualType = 'array' | 'matrix' | 'stack' | 'nodes' | 'graph' | 'bars' | 'bits';

export interface ProblemVisualizerScenario {
  visualType: ScenarioVisualType;
  phases: readonly string[];
  comments: readonly string[];
}

type ScenarioRow = [string, ScenarioVisualType, readonly string[], readonly string[]];

const row = (
  slug: string,
  visualType: ScenarioVisualType,
  phases: readonly string[],
  comments: readonly string[],
): ScenarioRow => [slug, visualType, phases, comments];

const rows: ScenarioRow[] = [
  row('maximal-square', 'matrix', ['base cells', 'extend square', 'best side = 2', 'area = 4'], ['Read the binary cell as the bottom-right corner of a candidate square.', 'The cell can grow only as far as its top, left, and diagonal neighbors allow.', 'The neighboring dynamic-programming values agree on a square of side two.', 'The largest side is two, so the returned area is four.']),
  row('lru-cache', 'nodes', ['cache empty', 'put A, put B', 'get A -> front', 'evict B'], ['The map and linked list start with no keys.', 'A and B are inserted with B as the most recently used node.', 'Reading A moves it ahead of B without changing lookup cost.', 'Adding the next key removes the tail, the least recently used entry.']),
  row('generate-parentheses', 'nodes', ['path = ""', 'open a pair', 'close only valid', '5 answers'], ['The search begins with zero opening and closing parentheses.', 'An opening parenthesis is legal while fewer than n opens have been used.', 'A closing parenthesis is allowed only when it cannot overtake the opens.', 'Every leaf is balanced, producing exactly five strings for n = 3.']),
  row('flood-fill', 'matrix', ['seed (1,1)', 'visit neighbors', 'recolor region', 'queue empty'], ['Start at the selected pixel and remember its original color.', 'Only same-color neighbors enter the frontier.', 'Each reachable pixel changes color once, so it cannot be processed twice.', 'When the frontier is empty, the connected component is completely filled.']),
  row('gas-station', 'bars', ['start = 0', 'tank rises', 'tank < 0 -> reset', 'start = 3'], ['The running tank measures whether the current candidate can reach the next station.', 'Positive gas-cost differences keep the candidate alive.', 'A negative tank proves every station before this index fails, so restart here.', 'The total circuit balance is nonnegative and station three is the valid start.']),
  row('single-number', 'bits', ['answer = 0', 'xor 4,1,2', 'pairs cancel', 'answer = 4'], ['XOR begins at zero, so the accumulator has no unmatched bits.', 'Each value toggles its bits in the accumulator.', 'Equal values toggle twice and disappear without extra storage.', 'Only four appears once, leaving four as the answer.']),
  row('reverse-bits', 'bits', ['read low bit', 'shift input', 'write mirror bit', '32 bits done'], ['The least significant bit is the next bit to move.', 'Shift the input right to expose the next source bit.', 'Shift the result left and append the exposed bit in reverse order.', 'After 32 iterations every bit occupies its mirrored position.']),
  row('path-sum-ii', 'nodes', ['path = [5]', 'choose 4', 'reach leaf 2', 'record paths'], ['The root enters the current path and reduces the remaining target.', 'The search descends through four and then eleven while preserving the path.', 'A leaf is recorded only when the remaining sum is exactly its value.', 'Backtracking restores the path before exploring the next branch.']),
  row('move-zeroes', 'array', ['write = 0', 'read non-zero', 'compact prefix', 'zeros at end'], ['The write pointer marks the next position for a non-zero value.', 'The read pointer scans every element, including zeros.', 'Swap each non-zero into the compact prefix without reordering it.', 'The untouched suffix contains only zeros.']),
  row('missing-number', 'bits', ['answer = n', 'xor index', 'xor value', 'missing = 2'], ['Seed the accumulator with n because the index range includes it.', 'XOR every expected index into the accumulator.', 'XOR every observed value; matching index/value pairs cancel.', 'The only bit pattern left is the missing index two.']),
  row('palindrome-partitioning', 'nodes', ['start = 0', 'choose "a"', 'choose "aa"', '2 partitions'], ['The recursion starts at the first character with an empty partition.', 'The single-character cut is a palindrome and opens one branch.', 'The two-character cut is also valid, creating the second branch.', 'Invalid cuts are skipped, leaving the two valid partitions.']),
  row('sum-of-two-integers', 'bits', ['a = 5, b = 7', 'xor sum bits', 'shift carry', 'sum = 12'], ['The two operands contain independent sum and carry information.', 'XOR adds positions without allowing carries to propagate.', 'AND followed by a left shift moves each carry into the next position.', 'When no carry remains, the XOR accumulator is twelve.']),
  row('same-tree', 'nodes', ['compare roots', 'values match', 'compare children', 'trees equal'], ['Both roots are present, so compare their values first.', 'Equal values keep the recursion alive for both subtrees.', 'Null structure is checked alongside values, not after the traversal.', 'Every corresponding node agrees, so the trees are identical.']),
  row('n-queens', 'graph', ['row 0', 'place (0,1)', 'reject diagonal', '2 boards'], ['Place exactly one queen in the first row and mark its column and diagonals.', 'The next row considers only columns that are not attacked.', 'The diagonal check prunes a branch before it reaches a dead end.', 'Complete placements are recorded, then undone to explore the second solution.']),
  row('contains-duplicate', 'array', ['seen = {}', 'add 1,2,3', 'see 1 again', 'duplicate = true'], ['The set starts empty before the scan.', 'Values one, two, and three are inserted as first occurrences.', 'The next one is already present, so the scan can stop immediately.', 'The repeated value proves the array contains a duplicate.']),
  row('group-anagrams', 'bars', ['empty buckets', 'key = aet', 'append words', '3 groups'], ['Each word is converted to a canonical frequency signature.', 'Eat, tea, and ate share the same key even though their order differs.', 'Tan and nat share another key while bat forms its own bucket.', 'Reading the bucket values returns three anagram groups.']),
  row('split-array-largest-sum', 'array', ['lo = 10, hi = 32', 'try mid = 21', '2 parts fit', 'answer = 18'], ['The lower bound is the largest single value and the upper bound is the total.', 'A candidate maximum of 21 can split the array into at most two groups.', 'Feasibility shrinks the upper half while preserving the answer interval.', 'Binary search converges on the smallest feasible maximum, eighteen.']),
  row('jump-game', 'bars', ['reach = 0', 'extend reach', 'index is reachable', 'last index true'], ['Reach begins at the first index.', 'Each jump extends the furthest boundary visible from the reachable prefix.', 'An index is safe whenever it lies at or before that boundary.', 'The boundary reaches the final index, so the array is traversable.']),
  row('course-schedule', 'graph', ['indegree = [1,0]', 'queue course 0', 'unlock course 1', 'processed = 2'], ['Indegree counts how many prerequisites still block each course.', 'Course zero has no blockers and enters the queue.', 'Removing it decrements course one to zero and unlocks it.', 'All courses are processed, proving there is no cycle.']),
  row('non-overlapping-intervals', 'bars', ['sort by end', 'keep [1,2]', 'remove [1,3]', 'removed = 1'], ['Sorting by finishing time exposes the interval that leaves the most room.', 'Keep the interval ending at two and advance the boundary.', 'The interval starting at one overlaps the boundary, so remove it.', 'The remaining intervals fit without overlap and only one removal is needed.']),
  row('valid-anagram', 'bars', ['count s', 'subtract t', 'all zero', 'same signature'], ['Count the characters in the first word.', 'Subtract each character from the second word as it is scanned.', 'No count becomes negative and every bucket returns to zero.', 'Both strings therefore have the same frequency signature.']),
  row('swap-nodes-in-pairs', 'nodes', ['first = 1', 'second = 2', 'rewire pair', '2 -> 1 -> 4 -> 3'], ['A dummy node makes the first pair behave like every later pair.', 'Save the second node before changing any links.', 'Point the predecessor to second, second to first, and first to the suffix.', 'Advance by two nodes and repeat for the remaining pair.']),
  row('valid-palindrome', 'array', ['left = A', 'skip punctuation', 'compare inward', 'palindrome = true'], ['The two pointers begin at the outside characters.', 'Non-alphanumeric characters are skipped without affecting the comparison.', 'Lowercase characters match as the pointers move toward the center.', 'The pointers cross without a mismatch, so the normalized text is a palindrome.']),
  row('climbing-stairs', 'array', ['one = 1, two = 1', 'step 2 -> 2', 'step 4 -> 5', 'step 5 -> 8'], ['There is one way to reach each of the first two stair states.', 'The next state is the sum of the two states immediately before it.', 'Only two rolling values are needed as the sequence advances.', 'The fifth stair has eight possible sequences.']),
  row('edit-distance', 'matrix', ['base row/col', 'match diagonal', 'edit mismatch', 'distance = 3'], ['Empty prefixes establish the first row and column of the table.', 'Matching characters inherit the diagonal cost without a new edit.', 'A mismatch chooses the cheapest insert, delete, or replace neighbor.', 'The bottom-right cell reports three edits from horse to ros.']),
  row('course-schedule-ii', 'graph', ['indegree ready', 'take 0', 'take 1', 'order found'], ['Build the prerequisite graph and start with every zero-indegree course.', 'Removing course zero exposes the next available course.', 'Each removal unlocks the next dependency in order.', 'The output contains every course; a cycle would have left the order short.']),
  row('word-ladder', 'graph', ['frontier = hit', 'change one letter', 'meet cog', 'length = 5'], ['Breadth-first search begins at the source word with distance one.', 'Generate only unseen words that differ by one character.', 'The frontier expands level by level until cog is reached.', 'The first time the target appears is the shortest transformation length.']),
  row('number-of-islands', 'matrix', ['scan land', 'sink island', 'skip water', 'islands = 1'], ['The outer scan finds the first unvisited land cell.', 'Flood-fill marks every connected land cell as visited.', 'Already-sunk cells and water are skipped by the scan.', 'One connected component produces one island count.']),
  row('coin-change', 'bars', ['dp[0] = 0', 'amount 2 -> 1', 'amount 6 -> 2', 'amount 11 -> 3'], ['Zero amount needs zero coins and seeds every later transition.', 'For amount two, coin two gives a one-coin solution.', 'The table reuses the best smaller amount rather than recomputing combinations.', 'Amount eleven is reached optimally with three five/one-value coins.']),
  row('longest-valid-parentheses', 'stack', ['stack = [-1]', 'push opening', 'pop matching', 'best = 4'], ['The sentinel boundary makes the first valid length measurable.', 'Opening indices wait in the stack for a matching close.', 'A close pops its opener; an empty stack resets the next boundary.', 'The widest valid span in the sample has length four.']),
  row('decode-ways', 'array', ['dp[0] = 1', 'read 2', 'read 22', 'ways = 3'], ['The empty prefix has one decoding and anchors the recurrence.', 'A valid single digit adds the previous prefix count.', 'The valid pair twenty-two adds the count from two positions back.', 'The string 226 has three possible decodings.']),
  row('reorder-list', 'nodes', ['find midpoint', 'reverse right half', 'weave halves', '1 -> 4 -> 2 -> 3'], ['Slow and fast pointers split the list into two halves.', 'Reverse the second half so its nodes arrive from the tail.', 'Alternate one node from the left and one from the reversed right.', 'The weave produces the required first-last ordering.']),
  row('find-k-closest-elements', 'array', ['window range 0..1', 'compare edges', 'shrink farther side', 'window = [1..4]'], ['Only windows of length k can be answers, so binary search their starting index.', 'Compare the left outside value with the right outside value around the window.', 'Discard the side farther from x while preserving sorted order.', 'The remaining window contains the four closest values.']),
  row('merge-k-sorted-lists', 'bars', ['push 3 heads', 'pop smallest', 'push next node', 'one sorted list'], ['The heap contains the current head of every non-empty list.', 'The smallest head is safe to append to the merged result.', 'Its successor replaces it in the heap, preserving the invariant.', 'The heap empties after all list nodes are linked in order.']),
  row('daily-temperatures', 'stack', ['waiting days', 'push 73', 'resolve 74', 'answers filled'], ['The monotonic stack stores indices that still need a warmer day.', 'Day zero waits because no warmer temperature has appeared.', 'A warmer temperature resolves every smaller temperature on top.', 'Unresolved indices receive zero after the scan reaches the end.']),
  row('valid-triangle-number', 'array', ['sort sides', 'fix largest', 'move left/mid', 'count = 3'], ['Sorting makes the largest side the right boundary of each search.', 'For a fixed largest side, compare the smallest and middle sides.', 'When the triangle inequality holds, every middle index in the range works.', 'Summing the valid ranges gives three triangles.']),
  row('subarray-sum-equals-k', 'array', ['prefix = 0', 'store prefix 1', 'find prefix - k', 'count = 2'], ['A frequency map starts with prefix zero so ranges from the beginning count.', 'Each value extends the running prefix sum.', 'A previous prefix equal to current minus k closes a target range.', 'The repeated prefix relationship appears twice in the sample.']),
  row('surrounded-regions', 'matrix', ['find border O', 'mark safe', 'flip enclosed', 'restore safe'], ['Border-connected O cells are the only ones that cannot be surrounded.', 'Flood-fill from each border O and mark those cells safe.', 'Every remaining O is enclosed and can be flipped to X.', 'Restore the temporary safe marker back to O.']),
  row('combination-sum', 'nodes', ['remaining = 7', 'choose 2', 'choose 3', '2 combinations'], ['The search starts with an empty path and the full target remaining.', 'Choose a candidate without advancing the index so it may be reused.', 'The path [2,2,3] reaches zero; the branch [7] does too.', 'The start index prevents duplicate orderings from being recorded.']),
  row('jump-game-ii', 'bars', ['end = 0', 'scan current layer', 'extend farthest', 'jumps = 2'], ['The current jump layer ends at index zero.', 'Every index in the layer contributes to the furthest next boundary.', 'When the scan reaches the boundary, commit one jump and extend it.', 'Two layers reach the last index in the sample.']),
  row('min-stack', 'stack', ['push -2', 'push 0', 'push -3', 'min = -3'], ['The value stack and minimum stack begin empty.', 'Each push stores the smaller of the new value and the previous minimum.', 'The third push updates the minimum without scanning older values.', 'getMin reads the top of the minimum stack in constant time.']),
  row('subsets', 'nodes', ['index = 0', 'exclude 1', 'include 1', '8 subsets'], ['Every value creates exactly two branches from the current path.', 'The exclusion branch advances without adding the value.', 'The inclusion branch adds the value before advancing.', 'Three binary choices produce eight terminal subsets.']),
  row('container-with-most-water', 'bars', ['left = 0, right = 8', 'area = 8', 'move shorter wall', 'max area = 49'], ['The initial container area is limited by its shorter wall.', 'Width shrinks on every move, so height must improve to beat the best area.', 'Move the shorter wall because moving the taller one cannot help.', 'The best pair holds forty-nine units of water.']),
  row('number-of-1-bits', 'bits', ['count = 0', 'clear low bit', 'clear again', 'count = 3'], ['The count starts at zero for the 32-bit input.', 'n & (n - 1) removes the lowest set bit in one operation.', 'Repeating the operation skips all zero runs.', 'Three operations are needed, so the number contains three one bits.']),
  row('rotate-image', 'matrix', ['original matrix', 'transpose', 'reverse rows', 'rotated clockwise'], ['The square matrix starts in row-major order.', 'Transpose swaps cells across the main diagonal.', 'Reversing every row converts the transpose into a clockwise rotation.', 'Every cell moved in place with constant extra storage.']),
  row('add-two-numbers', 'nodes', ['digits 2 + 5', 'carry = 0', 'digits 4 + 6', '807'], ['Two list pointers expose the least significant digits first.', 'Add the first pair and store the output digit with its carry.', 'Continue with the next pair, carrying overflow into the next node.', 'The linked result represents 807 in reverse-digit order.']),
  row('merge-intervals', 'bars', ['sort starts', 'take [1,3]', 'merge [2,6]', 'merged list'], ['Sort intervals by their starting point before scanning.', 'The current interval establishes the right boundary of the merged run.', 'An overlapping interval extends that boundary instead of creating a new row.', 'Disjoint intervals start a new merged segment.']),
  row('merge-two-sorted-lists', 'nodes', ['compare heads', 'take 1', 'advance pointer', 'merged order'], ['Two sorted heads are the only candidates for the next output node.', 'Link the smaller head and advance only that list.', 'Equal values preserve a valid sorted order while one pointer advances.', 'Append the untouched suffix after one list is exhausted.']),
  row('trapping-rain-water', 'bars', ['leftMax = 0', 'update wall', 'water above bar', 'total = 6'], ['The two-pointer scan tracks the highest wall seen from each side.', 'Advance the side with the lower boundary because it determines water depth.', 'A low bar stores the difference between the boundary and its height.', 'All trapped volumes add to six units.']),
  row('linked-list-cycle', 'nodes', ['slow = head', 'fast = head', 'fast laps slow', 'cycle found'], ['Slow advances one node while fast advances two.', 'If the list ends, fast reaches null and there is no cycle.', 'On a cycle, the faster pointer eventually catches the slower pointer.', 'The pointers meet, proving a cycle exists.']),
  row('redundant-connection', 'graph', ['components separate', 'union 1-2', 'union 2-3', 'edge 1-3 redundant'], ['Union-find starts with every vertex in its own component.', 'The first edges connect previously separate roots.', 'Path compression keeps later root checks shallow.', 'An edge whose endpoints already share a root is redundant.']),
  row('two-sum', 'array', ['map empty', 'store 2', 'need 7', 'indices found'], ['The hash map stores values seen before with their indices.', 'Value two is recorded before examining the next number.', 'For the current value nine, the complement seven is looked up.', 'The complement exists, so the pair is returned immediately.']),
  row('permutation-in-string', 'array', ['window length = 3', 'count s1', 'slide window', 'match found'], ['The window is fixed to the length of the target permutation.', 'Character counts for the target and first window are compared.', 'Sliding removes the outgoing character and adds the incoming one.', 'A matching frequency vector proves a permutation is present.']),
  row('word-search', 'matrix', ['start at cell', 'choose path', 'reject reused', 'word found'], ['DFS chooses a cell that matches the next character.', 'The cell is marked in the current path before exploring neighbors.', 'A visited cell cannot be reused in the same path.', 'The final character matches, so the word is found and the path unwinds.']),
  row('set-matrix-zeroes', 'matrix', ['scan zeros', 'mark row/col', 'zero marked lines', 'matrix updated'], ['First record which rows and columns contain a zero.', 'Markers preserve the original zero positions before any mutation spreads.', 'Zero every cell whose row or column was marked.', 'The matrix is updated without using a second full matrix.']),
  row('valid-parentheses', 'stack', ['stack empty', 'push opener', 'pop closer', 'valid = true'], ['Opening brackets wait on the stack for their matching closer.', 'The top records the most recent unresolved bracket.', 'A closer must match that top bracket before it is removed.', 'The stack is empty at the end, so every bracket is balanced.']),
  row('sliding-window-maximum', 'bars', ['window opens', 'deque decreasing', 'remove expired', 'maxima emitted'], ['The deque stores indices in decreasing value order.', 'Smaller values at the back can never become a future maximum.', 'Indices outside the current window leave from the front.', 'The front of each full window is the emitted maximum.']),
  row('longest-univalue-path', 'nodes', ['visit leaf', 'extend equal branch', 'join branches', 'best = 2'], ['DFS returns the longest same-value branch below each node.', 'A child branch extends only when its value equals the current node.', 'Two compatible branches can join through the current node.', 'The global best counts the longest same-value edge path.']),
  row('counting-bits', 'bits', ['offset = 1', 'reuse dp[0]', 'offset = 2', 'table complete'], ['The count for zero is the base case.', 'Numbers before the next power of two reuse an earlier offset.', 'When the offset doubles, values map back to the lower half of the table.', 'Every count is computed from a previously solved count.']),
  row('spiral-matrix', 'matrix', ['top boundary', 'walk right', 'walk down', 'inner layer done'], ['Four boundaries describe the unvisited rectangle.', 'Walk across the top row, then move the top boundary inward.', 'Walk down the right edge and continue shrinking the rectangle.', 'Repeat the remaining sides until every cell is in spiral order.']),
  row('path-sum', 'nodes', ['remaining = 22', 'subtract node', 'reach leaf', 'path exists'], ['The remaining target starts at twenty-two at the root.', 'Each recursive call subtracts the current node value.', 'Only a leaf can close the path and validate the remaining sum.', 'One root-to-leaf path reaches zero, so the answer is true.']),
  row('01-matrix', 'matrix', ['queue all zeros', 'distance = 0', 'expand ring', 'distances filled'], ['Every zero is a source and enters the queue at distance zero.', 'A multi-source BFS expands all nearest fronts simultaneously.', 'Each unvisited neighbor receives one more than its current cell.', 'The first distance assigned is always the nearest zero distance.']),
  row('house-robber', 'bars', ['prev2 = 0', 'rob house 0', 'skip or rob', 'loot = 12'], ['The rolling states represent the best loot two and one houses back.', 'The first house is the best choice available so far.', 'At each house compare skipping it with robbing it plus prev2.', 'The final rolling state is twelve without robbing adjacent houses.']),
  row('insert-interval', 'bars', ['before new', 'overlap [2,5]', 'merge end', 'inserted list'], ['Intervals ending before the new start can be copied directly.', 'Overlapping intervals expand the new interval on both boundaries.', 'Once overlap ends, append the merged interval exactly once.', 'The untouched suffix follows in sorted order.']),
  row('network-delay-time', 'graph', ['dist[source] = 0', 'pop nearest', 'relax edge', 'max delay = 2'], ['Dijkstra starts with the source at time zero.', 'The heap always finalizes the not-yet-processed node with smallest time.', 'Relax an outgoing edge only when the new route is shorter.', 'The maximum finalized distance is the time for the signal to reach all nodes.']),
  row('sort-colors', 'bars', ['low = 0, mid = 0', 'move 0 left', 'move 2 right', 'colors sorted'], ['Three boundaries divide the array into zero, unknown, and two regions.', 'A zero swaps into the low region and both low and mid advance.', 'A two swaps with high while mid stays to inspect the incoming value.', 'When mid passes high, every color is in its correct region.']),
  row('task-scheduler', 'bars', ['count tasks', 'place A slots', 'fill gaps', 'intervals = 8'], ['Frequency counts reveal the task that creates the largest cooldown frame.', 'Place the most frequent task first to define the minimum frame.', 'Other tasks fill idle gaps wherever possible.', 'The larger of work time and frame time is eight intervals.']),
  row('unique-paths', 'matrix', ['top row = 1', 'add from left', 'add from top', 'ways = 28'], ['The first row and column have one path because movement is one-way.', 'Each interior cell receives paths arriving from its left.', 'Add the paths arriving from above to complete the recurrence.', 'The bottom-right cell contains twenty-eight paths.']),
  row('reverse-linked-list', 'nodes', ['prev = null', 'save next', 'reverse link', 'new head = 5'], ['The previous pointer starts outside the list.', 'Save current.next before changing the direction of the link.', 'Advance both pointers after reversing one edge.', 'When current becomes null, prev points at the new head.']),
  row('permutations', 'nodes', ['index = 0', 'swap 1 into place', 'recurse suffix', '6 permutations'], ['The next output position is selected by swapping each remaining choice into it.', 'Place one at the current index and recurse on the suffix.', 'Undo the swap before trying the next candidate.', 'Three choices across the positions produce six permutations.']),
  row('binary-search', 'array', ['lo = 0, hi = 5', 'mid = 2', 'mid = 4', 'index = 4'], ['The target is kept inside the inclusive sorted interval.', 'The first midpoint is too small, so discard its left half.', 'The next midpoint equals the target and closes the search.', 'The target nine is at index four.']),
  row('longest-increasing-subsequence', 'bars', ['tails = []', 'tail = 10', 'replace tail', 'length = 4'], ['The tails array stores the smallest ending value for each length.', 'Ten starts a subsequence of length one.', 'A smaller tail replaces a larger one without changing the best length.', 'The number of tails is four, the LIS length.']),
  row('decode-string', 'stack', ['read repeat 3', 'push prefix', 'close inner', 'accaccacc'], ['Digits accumulate into a repeat count before the opening bracket.', 'The current prefix and count wait on stacks while the nested text is read.', 'A closing bracket restores the prefix and repeats the nested result.', 'The outer repeat expands the decoded string three times.']),
  row('palindrome-linked-list', 'nodes', ['find midpoint', 'reverse second half', 'compare nodes', 'palindrome = true'], ['Slow and fast pointers locate the midpoint of the list.', 'Reverse the second half so it can be compared from the tail backward.', 'Compare corresponding values from both halves.', 'All pairs match; the list is a palindrome.']),
  row('word-break', 'array', ['dp[0] = true', 'match leet', 'match code', 'can segment = true'], ['The empty prefix is reachable and anchors dictionary transitions.', 'The dictionary word leet marks its ending position reachable.', 'From that reachable boundary, code marks the final position.', 'The final dp value is true, so the string can be segmented.']),
];

export const problemVisualizerScenarios: Record<string, ProblemVisualizerScenario> = Object.fromEntries(
  rows.map(([slug, visualType, phases, comments]) => [slug, { visualType, phases, comments }]),
);
